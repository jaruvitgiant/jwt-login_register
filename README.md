# NodeJS JWT Authentication Project

โปรเจกต์ระบบยืนยันตัวตน (Authentication) ด้วย **JSON Web Token (JWT)** โดยใช้ **Express.js** และ **MongoDB**

## 🚀 ฟีเจอร์หลัก
* สมัครสมาชิก (Register) พร้อมการเข้ารหัสรหัสผ่านด้วย `bcryptjs`
* เข้าสู่ระบบ (Login) และออก Token (JWT)
* ระบบ Middleware สำหรับตรวจสอบความถูกต้องของ Token (Auth Guard)
* การตั้งค่าผ่าน Environment Variables (.env) เพื่อความปลอดภัย

---

## 🛠 การติดตั้ง (Installation)

1.  **Clone โปรเจกต์:**
    ```bash
    git clone <your-repository-url>
    cd jwt-project
    ```

2.  **ติดตั้ง Dependencies:**
    ```bash
    npm install
    ```

3.  **ตั้งค่า Environment Variables:**
    สร้างไฟล์ชื่อ `.env` ไว้ที่ root ของโปรเจกต์ และคัดลอกค่าจาก `.env.example` ไปใส่:
    ```env
    PORT=4001
    MONGO_URL=mongodb://127.0.0.1:27017/jwt_db
    TOKEN_KEY=your_secret_key_here
    ```

4.  **เริ่มรันโปรเจกต์:**
    ```bash
    # สำหรับ Development (ใช้ nodemon)
    npm run dev

    # สำหรับ Production
    npm start
    ```

---

## 🔑 รายละเอียด API (API Endpoints)

### 1. สมัครสมาชิก (Register)
* **URL:** `/register`
* **Method:** `POST`
* **Body (JSON):**
    ```json
    {
      "first_name": "John",
      "last_name": "Doe",
      "email": "john@example.com",
      "password": "password123"
    }
    ```

### 2. เข้าสู่ระบบ (Login)
* **URL:** `/login`
* **Method:** `POST`
* **Body (JSON):**
    ```json
    {
      "email": "john@example.com",
      "password": "password123"
    }
    ```

### 3. ทดสอบ Route ที่ต้องใช้ Token (Protected Route)
* **URL:** `/welcome`
* **Method:** `POST`
* **Headers:**
    * `Authorization`: `Bearer <YOUR_TOKEN_HERE>`

---

## 📁 โครงสร้างโฟลเดอร์
* `config/` - การเชื่อมต่อฐานข้อมูล
* `middleware/` - ฟังก์ชันตรวจสอบ JWT Token
* `model/` - Mongoose Schema (User Model)
* `app.js` - การตั้งค่า Express และ Routes
* `index.js` - จุดเริ่มต้นของโปรแกรม (Server Entry Point)

---

## ⚠️ ความปลอดภัย
ไฟล์ `.env` จะถูกข้ามโดย `.gitignore` เพื่อความปลอดภัย **ห้ามนำข้อมูลสำคัญขึ้น GitHub เด็ดขาด**
